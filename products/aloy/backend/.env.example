# Local single-developer default. Hosted and multi-user deployments must use
# Postgres through an async URL from the deployment's secret manager.
DATABASE_URL=sqlite+aiosqlite:///./aloy_backend.db

# Required. Used to verify Supabase JWTs (auth).
SUPABASE_URL=https://xxxx.supabase.co

# Comma-separated list of allowed origins for browser requests.
# Include the Vercel prod URL, any preview URLs you want, and localhost dev.
CORS_ORIGINS=https://your-app.vercel.app,http://localhost:5173

# LLM provider credentials (whichever the agent config selects).
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
FIREWORKS_API_KEY=
SERPER_API_KEY=
SERPAPI_API_KEY=
TAVILY_API_KEY=

# Developer-owned Aloy Conversation/Builder/Critic role assignments.
ALOY_MODEL_ROLES_PATH=aloy.models.yaml

# Optional tunables (defaults shown).
LOG_LEVEL=INFO
RATE_LIMIT_RPM=60
MAX_CONCURRENT_RUNS=5

# Global template publication is fail-closed. For local Governance testing,
# list only the authenticated operator subject ids explicitly trusted to change
# the shared catalog. Never infer this authority from organization ownership.
EVENT_TEMPLATE_CATALOG_OPERATOR_SUBJECTS=

# Surface compilation is isolated by default. A developer without a remote
# sandbox may opt into the fixed host-local compiler on their own workstation.
# Never use local_dev in a hosted or multi-user deployment.
SURFACE_BUILD_BACKEND=isolated

# Account connections (native OAuth — Gmail, etc.). Generate the enc key with:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
CONNECTIONS_ENC_KEY=
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
BACKEND_BASE_URL=http://localhost:8000
APP_BASE_URL=http://localhost:5173

# Object storage. 'local' (default) keeps blobs on this machine's disk under
# STORAGE_DIR — fine for dev/single-node. 's3' uses any S3-compatible store
# (needs boto3: pip install boto3). For Supabase Storage: create a bucket,
# then Project Settings -> Storage -> S3 access keys.
STORAGE_BACKEND=local
# STORAGE_S3_ENDPOINT=https://xxxx.supabase.co/storage/v1/s3
# STORAGE_S3_BUCKET=aloy-files
# STORAGE_S3_ACCESS_KEY=
# STORAGE_S3_SECRET_KEY=
# STORAGE_S3_REGION=us-east-1
