# products/aloy/desktop — desktop shell (later)

Planned desktop surface (Electron or similar). Talks to the Aloy backend over
**REST + SSE**, same as `products/aloy/app`. Not a Python package; harvest the Hermes
desktop shell (`products/aloy/desktop`) and retarget it to REST + SSE when we get here.
