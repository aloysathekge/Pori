services:
  pori:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    # Optional: mount a workspace for filesystem tools
    # volumes:
    #   - ./workspace:/app/workspace
