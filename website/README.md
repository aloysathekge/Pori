# website — public marketing site

**Home for the contents of `../pori_website`** (external git repo): the public Vite/React/shadcn marketing site.

Copy the `pori_website` contents here (source repo left untouched; history not preserved).

This is a standalone public site — not a product surface and not part of the Python import graph.
