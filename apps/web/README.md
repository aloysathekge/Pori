# apps/web — web surface

**Home for the contents of `../../../pori_cloud_client`** (external git repo): the Vite/React/TypeScript frontend.

Copy the `pori_cloud_client` contents here (source repo left untouched; history not preserved). Then wire it to the Aloy backend over **REST + SSE** — never by Python import.

This is a **surface**, not a Python package; it is not part of the `products → extensions → pori` import graph.
