{
  "name": "@aloy/shared",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Aloy shared transport — a typed REST + SSE (PoriEvent) client for the Aloy backend, consumed by the web and desktop surfaces.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc -p . --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
