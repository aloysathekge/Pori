# apps/desktop — desktop shell (later)

Planned desktop surface (Electron or similar). Talks to the Aloy backend over
**REST + SSE**, same as `apps/web`. Not a Python package; harvest the Hermes
desktop shell (`apps/desktop`) and retarget it to REST + SSE when we get here.
